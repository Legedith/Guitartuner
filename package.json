{
  "name": "fretline-tuner",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "A private guitar and ukulele tuner with tuning-aware chords and a bundled play-along library.",
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/index-playlist.mjs && node --check src/boot.js && node --check src/chords.js && node --check src/pitch.js && node --check src/reference-string.js && node --check src/song-library.js && node --check src/tunings.js && node --check src/app-parts/*.js && node --check service-worker.js"
  },
  "engines": { "node": ">=20" }
}
